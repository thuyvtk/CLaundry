<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="thuyvtk.activity.laundry_customer">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.DetailOrderHistoryActivity"></activity>
        <activity android:name=".activity.RatingActivity" />
        <activity android:name=".activity.SearchStoreResultActivity" />
        <activity android:name=".activity.ReceiptDetailActivity" />
        <activity android:name=".activity.ShoppingCartActivity" />
        <activity android:name=".activity.StoreDetailActivity" />
        <activity android:name=".activity.VerifyCodeOTPActivity" />
        <activity android:name=".activity.LoginByPhoneActivity" />
        <activity android:name=".activity.EditProfileActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>